#!/bin/bash

tmp=$IFS
IFS=";"
cat notes.csv | while read prenom nom mail note ; do
    message=$(sed "/<NOM>/$nom/; /<PRENOM>/$prenom/; /<NOTE>/note/;" gabarit.txt)
    echo $message | bash mock_mail -s "note outils systeme" $mail
done
